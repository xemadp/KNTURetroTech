% Defines the KNTURetroTech official university colors for web themes
\usepackage{xcolor}

% KNTURetroTech’s primary color.
% Used for design elements, navigation bars, nonlinked headers, and backgrounds.
% Not used for links.
\definecolor{rt-teal}{HTML}{007777} % TEAL GREEN

% Accent color.
% Used in alternating boxes component.
\definecolor{rt-blue-dark}{HTML}{002FA7} % DARK BLUE

% Background color.
\definecolor{rt-page-bg}{HTML}{F7F7F7}

% Background and accent color
\definecolor{rt-gray-blue}{HTML}{DADFE1}

% Design element background color and accent color.
\definecolor{rt-gray-darker}{HTML}{646569}

% Design element background color, accent color, and footer background color.
% Often used in place of black.
\definecolor{rt-gray-darkest}{HTML}{282728}

% Used for standard text on a white background.
\definecolor{rt-body-font}{HTML}{494949}

% Used for links in standard text on a white background.
\definecolor{rt-anchor-color}{HTML}{0479A8}

% Used for footer text.
\definecolor{rt-gray-footer-text}{HTML}{ADADAD}

% Used for links in footer text.
\definecolor{rt-gray-footer-text-hover}{HTML}{F9F9F9}
